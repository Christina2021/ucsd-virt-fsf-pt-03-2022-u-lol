import GroceryList from './components/GroceryList';

// What are we building?  A grocery list from scratch! We will create a component called Grocerylist and import it into this file so that it may be rendered.

function App() {
  return (
    <div>
      <GroceryList />
    </div>
  );
}

export default App;
